set(PACKAGE_VERSION "@VERSION_SHORT@")
if("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@VERSION_SHORT@")
    set(PACKAGE_VERSION_EXACT 1)
endif()
if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @VERSION_MAJOR@)
    set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
